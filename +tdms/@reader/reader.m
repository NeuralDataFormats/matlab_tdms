classdef reader
    %
    %
    %
    %1) read start
    %2) channel
    %3) data type
    %4) interleaved
    
%     obj.raw_n_bytes_read    = raw_n_bytes_read;
% obj.raw_n_values_read   = raw_n_values_read;
% obj.raw_index_start     = raw_index_start;
% obj.raw_chan_id         = raw_chan_id;
% obj.n_seg_reads         = n_seg_reads;

    %??? Is this the main object?
    %1) create reader (filepath,options)
    %   - this instantiates meta reading
    %2) read data
    %     - from reader  - minimal control
    %     - from group   - some control
    %     - from channel - lots of control

    
    properties
    end
    
    methods
    end
    
end

